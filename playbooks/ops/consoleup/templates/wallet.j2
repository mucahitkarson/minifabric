{% set filepath=pjroot+'/vars/keyfiles/'+orgattrs[item].certpath+'/'+item+'/users/admin@'+item+'/msp/' %}
{
    "name": "admin@{{ item }}",
    "type": "identity",
    "private_key": "{{ lookup('file', filepath+'keystore/priv_sk')|b64encode }}",
    "cert": "{{ lookup('file', filepath+'signcerts/admin@'+item+'-cert.pem')|b64encode }}"
}
